language: erlang
env:
  - LUA=""
#  - LUA="luajit"
branches:
  only:
    - master
    - develop
install:
#  - sudo apt-get install luajit
  - sudo apt-get install apt-file
  - apt-file update
  - apt-file search libyaml.so
  - apt-file search libyaml.a
  - sudo apt-get install luarocks
  - sudo apt-get install libyaml-dev
  - sudo apt-get install libyaml-0-2
  - apt-cache search libyaml
  - dpkg -l
  - find / -name libyaml.so
  - find / -name libyaml.a
  - sudo luarocks install luafilesystem
  - sudo luarocks install busted
  - sudo luarocks install luasocket
  - sudo luarocks install luacrypto
  - sudo luarocks install lyaml
script: "busted spec"

#notifications:
#  webhooks:
#    - http://my-url/travis-results
#  recipients:
#    - engineering@company.com
#  email:
#    on_success: change
#    on_failure: always
